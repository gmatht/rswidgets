[package]
name = "rustxwidgets"
version = "0.1.0"
edition = "2021"

[dependencies]
once_cell = "1.19"
thiserror = "1.0"
libloading = "0.8"

[target.'cfg(target_os = "linux")'.dependencies]
gtk_dynamic_loader = { path = "../gtk_dynamic_loader" }

[target.'cfg(windows)'.dependencies]
native-windows-gui = "1.0"

[features]
default = []
